# DEPRECATED: This workflow is no longer active.
# The tracking site was archived as of 2025-12-31. Data now lives in Cloudflare R2.
# Kept for reference. See daily-data-update.yml for the current pipeline.
name: Update Job Data Weekly (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
    - name: Deprecated
      run: echo "This workflow is deprecated. Archived as of 2025-12-31. See daily-data-update.yml."