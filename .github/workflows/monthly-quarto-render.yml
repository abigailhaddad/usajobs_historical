# DEPRECATED: This workflow is no longer active.
# Data moved from Git LFS to Cloudflare R2 and the Quarto source was removed.
# Kept for reference. See daily-data-update.yml for the current pipeline.
name: Monthly Quarto Render (DEPRECATED)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
    - name: Deprecated
      run: echo "This workflow is deprecated. Data now lives in Cloudflare R2. See daily-data-update.yml."